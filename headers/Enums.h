#ifndef ENUMS_H
#define ENUMS_H

enum CellType {
    GRASS,
    ROAD
};

enum MoveDirections {
    TOP,
    RIGHT,
    BOTTOM,
    LEFT
};

enum Resources {
    WOOD,
    STONE,
    IRON,
    GOLD
};

#endif